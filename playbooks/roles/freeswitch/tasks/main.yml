- name: Install FreeSWITCH
  apt:
    name: freeswitch
    state: present

- name: Deploy configs
  template:
    src: sip_profiles.xml.j2
    dest: /etc/freeswitch/sip_profiles/sip_profiles.xml

- name: Restart FreeSWITCH
  service:
    name: freeswitch
    state: restarted
